# PRD 3 — 기능 요구사항 (FR)

- Status: Draft (V1)
- Last updated: 2026-09-24
- 이 문서는 `docs/prd/` 묶음의 한 조각이다 — 전체 지도는 [`docs/prd/README.md`](README.md).

## Functional Requirements

FR-1~FR-18 은 V0.5(축제 운영본) 기준이고, FR-19~FR-32 가 V1 에서 더해진다. V0.5 요구 중 V1 이 대체하는 것은
해당 줄 끝에 **V1** 표시로 어느 FR 이 이어받는지 적었다 — 번호는 Phase PLAN·ADR 이 참조하므로 다시 매기지 않는다.
V1 화면 근거는 Figma `imSnlOGTqwtPhGyzhA8yc9`(v1.0), 백엔드 결정 근거는 2026-09-22 BE 회의록이다.

`Area` 열은 Notion ⚔️ PRD 보드의 `구분` 이 된다 — 새 FR 을 적을 때 함께 채운다(값은 보드의 선택지와 같아야
한다: 진입/홈 · 사주 · 궁합지도/로그인 · 공유/친구궁합 · 소개팅 · 재화). 보드의 담당자·상태·FE·BE·수용 기준은
사람이 관리하고 동기화가 덮어쓰지 않는다 (ADR-20260923-prd-single-notion-db).

`담당` 열은 보드 `담당자` 의 **사본**이다 — 원본은 Notion 이고 동기화가 이 열을 읽지도 쓰지도 않으므로,
배정이 바뀌면 보드를 먼저 고치고 이 표를 손으로 맞춘다. V0.5 에서 이미 끝난 FR 과 아직 배정하지 않은 FR 은 `—`
로 둔다. 2026-09-23 V1 배정 기준이다.

디자인(「UI 최종 - 개발용」)과 이 문서가 어긋나면 **디자인이 우선한다**(2026-09-12 결정). 아래 FR은 그 원칙으로 디자인에 맞춰 고친 것이다. 단, 2026-09-13 기획 피드백으로 확정한 항목(FR-3 등급 체계·행운의 장소, FR-5 등급 표기, FR-9·FR-12 열람권과 오픈일, FR-15 사주 요약 제외)은 디자인보다 우선하며, 어긋나는 디자인(인연카드의 0–100 바)은 디자인을 고친다.

| ID    | Requirement                                                                                                                | Area | Priority | Related | 담당 |
|-------|----------------------------------------------------------------------------------------------------------------------------|------|----------|---------|------|
| FR-1  | 시스템은 `/` 첫 방문에 8초 인트로 영상을 보여줘야 한다: 처음 2초는 건너뛰기 칸에 숫자 카운트다운(2 → 1)이 보이고 2초부터 건너뛰기 버튼이 나타나며, 영상이 끝나거나 건너뛰면 사주 입력으로 넘어가며 이후 방문에는 보이지 않는다. 다른 경로로 들어오면 보이지 않는다. 공유 링크 입력(SCR-06)으로 처음 들어온 방문자에게도 보인다 **V1(2026-09-24)**: 첫 방문에는 메인 티저를 보인다 — 제목 '운명도 꿰어야 사랑이다'와 서비스 소개 아래 세 가지 진입을 둔다. '내 사주 보기'는 사주 입력(SCR-02)으로 가되 이 브라우저에 사주 결과가 있으면 로그인하지 않아도 그 결과(홈, SCR-04)로 간다. '새로운 인연 찾기'는 소개팅(SCR-15)으로 가고 로그인·프로필 조건은 소개팅 쪽이 판단한다(FR-24). '이미 아이디가 있어요'는 카카오 로그인(FR-20)으로 가고 로그인 뒤 계정의 기존 기록을 불러온다(FR-21) — 도착 화면은 Q22. 티저에는 하단 네비를 보이지 않고, 첫 방문인지 판단할 수 없으면 티저를 보인다. 인트로 영상과 티저의 순서는 Q23(기능명세서 1.1~1.4, Figma 「0. 메인 진입 티저」) | 진입/홈 | Could    | US-1 | 이정진 |
| FR-2  | 시스템은 사주 입력을 받아야 한다: 성별(남자/여자) · 달력 기준(양력/음력) · 생년월일(숫자 8자리 YYYYMMDD) · 음력이면 윤달 여부 · 태어난 시간(12시진 선택 또는 '몰라요' — 자시는 00:00–01:30·23:30–24:00 두 칸이라 13칸, 값은 칸의 가운데 시각) · 닉네임(1–8자). 태어난 지역은 받지 않는다(사주에 영향이 작아 2026-09-13 제외 — 백엔드 계약에서도 빠졌다). 필드별 검증 실패 문구를 띄우고, 연결 실패 시 입력값을 유지한다. '점지 확인하기' 는 필수값을 모두 채운 뒤에만 활성화되고, 연속 클릭에 의한 중복 요청을 막는다(기능명세서 2.1·2.8) | 사주 | Must | US-1 | 이동건 |
| FR-3  | 시스템은 사주 결과를 운명 카드로 보여줘야 한다: 십이간지 보살 캐릭터, 운명 제목·설명(운명 카드 문구 영역 — 설명은 공백 포함 140자 이내(Figma 658:5130 주석), 제목은 한 줄에 들도록 글자 크기를 줄이고, 둘 다 말줄임 없이 전문을 보인다. 2026-09-15), 연애운·결혼운·자녀운 3영역(문자 등급 6단계 SS·S·A+·A·B+·B + 보살 말투 문구), 음양오행(사주 원국의 목·화·토·금·수 개수를 오행마다 8칸 막대로 — Figma 982:3446, 2026-09-17), 행운의 장소(동국대 캠퍼스 안)·행운의 아이템. 결과 대기 중 로딩 상태, 실패 시 에러 상태를 갖는다 **V1(2026-09-22)**: 해설 카테고리가 연애운·결혼운·자녀운으로 확정되고, 잘 맞는 오행과 그 이유가 추가되며, 그 아래 'OO 기운의 사람 만나보기' 로 소개팅(FR-24)으로 넘어간다 — 로그인·프로필 등록 조건은 소개팅 쪽에서 판단한다(기능명세서 3.6). 구 카테고리로 만든 기존 결과(약 905건)는 지우지 않으므로 새 항목이 비어 올 수 있다 — 비면 그 영역을 그리지 않는다 | 사주 | Must | US-1 | 이동건 |
| FR-4  | 시스템은 '친구에게 공유' 버튼으로 공유 링크(`/s/:shareId`)를 공유할 수 있게 해야 한다. 결과 화면에서는 친구 궁합 순위가 비어 있으면 안내 아래(Figma 796:3828), 있으면 목록 아래(Figma 796:3885, 2026-09-17)에 둔다. 궁합 지도에서도 공유할 수 있다. 공유 방식(OS 공유 시트 · 링크 복사 · 완료 안내)은 디자인·기획서에 없어 미정 | 공유/친구궁합 | Must | US-2 | 강근우 |
| FR-5  | 시스템은 결과 화면(SCR-04)의 운명 카드(앞면: 캐릭터 + 연애운·결혼운·자녀운 문자 등급 SS~B 6단계 — 수치가 아니라 등급, 뒷면: "운명도 꿰어야 사랑이다")에 '카드 뒤집기' 버튼을 두어 앞뒤를 전환하고(결과 화면에 들어오면 뒷면부터 보인다), 카드 바로 아래 '카드 저장하기'(Figma 796:3862, 2026-09-17 '인스타 스토리 공유하기'에서 바뀜)로 카드 앞면을 이미지로 만들어 Web Share(파일)로 넘겨 기기에 저장하게 해야 한다. 별도 인연카드 화면은 없다(2026-09-15) | 사주 | Must | US-2 | 강근우 |
| FR-6  | 시스템은 공유 링크로 들어온 사용자의 궁합을 만들어 링크 주인의 궁합 지도로 보내야 한다: SCR-06 에서 사주를 입력해 '운명 지도 확인하기'를 누르면 결과 대기(SCR-03)를 보여주며 결과(`POST /results`)와 궁합(`POST /shares/{shareId}/compatibility`)을 차례로 만들고 SCR-13 으로 간다. 이 브라우저에 내 결과가 이미 있으면 링크로 들어올 때 입력 없이 궁합을 만들고 SCR-13 으로 간다 — 단 이 탭에서 이미 궁합을 만든 뒤 뒤로가기로 돌아온 입력 화면은 폼을 보여주고, 다시 제출하면 새 결과를 만든다(주인 지도에 한 번 더 들어간다). 자기 링크(`SELF_COMPATIBILITY`)면 궁합 없이 자기 결과(SCR-04)로 가고, 궁합 생성이 실패하면 오류를 안내하며 다시 시도는 입력 없이 궁합만 다시 부른다. SCR-13 에는 뒤로가기가 없다. '내 사주 내용도 확인하기'는 자기 결과(SCR-04)로 가고, 그렇게 들어온 SCR-04 에만 맨 위 '뒤로가기'가 있어 SCR-13 으로 돌아간다 — 지도와 내 사주를 오갈 수 있다(2026-09-15 Figma 개정, 720:3668·720:3587) | 공유/친구궁합 | Must | US-3 | 강근우 |
| FR-7  | 시스템은 궁합 점수를 네 등급으로 표기해야 한다 — 귀인 90 이상 · 찰떡 75–89 · 벗 61–74 · 스침 60 이하. 등급 값은 백엔드가 주며 화면이 계산하지 않는다 | 공유/친구궁합 | Must | US-3 | 이정진 |
| FR-8  | 시스템은 궁합 지도 화면에 친구를 구슬(등급별 색)로 그리고, 등급별 인원 수와 친구 궁합 순위 목록을 누적해 보여줘야 한다. 구슬은 자기 등급 색 궤도 위에 놓인다 — 달에서 가까운 줄부터 귀인·찰떡·벗·스침(2026-09-15 소유자 결정). 결과 화면 하단에는 순위 요약을 두고, 인연이 없을 때는 "아직 인연이 없어요" + 공유 CTA를 보여준다. 결과 화면 순위 요약 제목 줄 오른쪽의 '지도 보기 >'는 궁합 지도(SCR-08 `/me/map`)로 이동한다(Figma 798:3138). 순위는 점수 높은 순이다. 지도는 움직인다(2026-09-15 소유자 결정): 궤도 선은 늘 제자리에서 30초에 한 바퀴 돌고, 친구 3명 이상이면 구슬이 자기 궤도의 지도 패널 안 구간(패널 가장자리까지, 제목 뒤로 지나갈 수 있다)을 10초 동안 흐른 뒤 10초 동안 숨었다가 다시 나온다 — 구간 양 끝 10° 에서 서서히 옅어져 바깥 궤도일수록 길게 옅어진다 — 같은 궤도 구슬은 친구마다 고정된 랜덤 자리(닉네임으로 정해 새로고침해도 같다)에서 서로 44px 이상 떨어져 출발해 겹치지 않고, 한 궤도에 그 간격이 들어가지 않을 만큼 많으면 보이는 10초는 그대로 두고 숨는 시간을 늘린다. 구슬 수 제한은 없고(2026-09-15), 닉네임은 똑바로 선다. 기기의 '동작 줄이기' 설정이면 멈춘다 | 궁합지도/로그인 | Must | US-4 | 이정진 |
| FR-9  | 시스템은 결과 화면 맨 아래에 소개팅 사전신청 섹션(873:4155 — 오픈일·서비스명과 '사전 신청하고 알림 받기')을 두고, 누르면 사전신청 모달(SCR-09)을 열어야 한다. 사전신청(작성·동의) 완료가 곧 상대 정보 열람권이다. 사전신청 전에는 상대 정보가 화면·응답 어디에도 없어야 한다 **V1**: 사전신청 모달 대신 소개팅 진입(FR-19)으로 바뀐다. 이 줄은 V0.5 운영본 기준이다 | 소개팅 | Must | US-5 | 이동건 |
| FR-10 | 시스템은 사전신청 모달에서 이름 · 이메일 · 연락처 · 학과 · MBTI(16유형 선택) · 자기소개 · 성별 · 선호 성별을 입력받아야 하며, 기본/오류/로딩/실패/완료 상태를 갖는다. 연락처는 전화번호 또는 인스타그램 아이디 **택1**이다(디자인의 세그먼트 그대로 — 백엔드가 `contactMethod` 하나와 값 하나를 저장한다). 성별·선호 성별은 `POST /signups` 필수값인데 디자인에 없어 폼에 세그먼트 두 줄로 넣는다(2026-09-16 결정). 사진은 받지 않는다(2026-09-17 결정 — 디자인의 '사진 추가' 칸을 뺀다) | 소개팅 | Must | US-5 | 이동건 |
| FR-11 | (MVP 제외) 알림 신청 완료 시 1회 쿠폰 지급 사실을 보여준다 — 디자인 없음, Non-goals 참고                                     | 재화 | Could    | US-5 | 이동건 |
| FR-12 | 시스템은 백엔드가 궁합 점수를 바탕으로 제공하는 소개팅 후보 목록을 점지 카드 형태로 보여주되, 내가 입력한 항목에 대응하는 상대 정보만 노출해야 한다. 소개팅 후보 화면은 축제 당일(2026-09-29)부터 열리며, 그 전 접근 시 안내 상태를 보여준다(오픈 판정 주체는 Q13). 화면 디자인은 미정(SCR-10) **V1**: FR-26(Top 3 추천)·FR-27(리롤)·FR-28(해금)이 이 요구를 대체한다 | 소개팅 | Must | US-6 | 이동건 |
| FR-13 | 시스템은 '운명의 실 보내기'와 상대의 '당기기'를 처리하고, 양측이 성립하면 서로의 전화번호와 (등록한 경우) 인스타그램 아이디를 공개해야 한다. 성립 후 대화는 앱 밖에서 한다 — 앱 내 채팅 없음(2026-09-13 확정, Non-goals). 화면 디자인과 API 형태는 미정(SCR-11) **V1**: FR-29(운명의 실)·FR-30(요청함)이 이 요구를 대체한다 | 소개팅 | Must | US-6 | 강근우 |
| FR-14 | 시스템은 공유 링크로 들어온 사람의 궁합을 링크 주인의 궁합 지도·순위에 누적해야 한다 — 궁합 지도(SCR-08) 맨 아래 '친구에게 공유하고 궁합 지도 넓히기'가 `/s/:shareId` 를 공유한다(Figma 558:2571) | 공유/친구궁합 | Must | US-3 | 강근우 |
| FR-15 | 시스템은 SCR-06 부제에 "아래 정보를 입력하고 나와 <링크 주인 닉네임> 님의 귀인 궁합을 관계로 확인해보아요."(Figma 720:3653 문구 그대로)를 보이고, SCR-13 에 링크 주인의 궁합 지도(주인 닉네임 제목, 부제 "닉네임님과의 궁합 지도예요.", 구슬·등급별 인원·친구 궁합 순위)를 보여줘야 한다(Figma 720:3668). 두 화면 모두 주인의 사주 요약(운명 제목·설명·등급·십이간지·행운)은 제공하지 않는다. 주인에게 친구가 없으면 순위 빈 상태는 SCR-08 과 같다. 없는 링크는 오류 상태를 보여준다 **V1(2026-09-23)**: 공유 링크로 들어오면 링크 주인의 궁합지도와 함께 'OO님의 궁합지도에 초대됐어요' 초대 티저를 보이고, 없거나 만료된 링크의 오류 상태에는 홈으로 가거나 다시 들어올 경로를 함께 둔다(기능명세서 5.1·5.16 — 5.16 은 디자인 미정) | 공유/친구궁합 | Must | US-3 | 강근우 |
| FR-16 | 시스템은 Web Share를 지원하지 않는 브라우저에서 인연카드를 이미지로 저장하는 폴백을 제공해야 한다                         | 공유/친구궁합 | Must     | US-2 | 강근우 |
| FR-17 | 시스템은 사전신청 모달에서 수집 항목·이용 목적·보관 기간을 고지하고 동의를 받아야 하며, 동의 없이는 추가 정보를 전송하지 않아야 한다. 동의 UI는 디자인시스템 PreRegistrationForm의 OptionalConsent 슬롯을 켜서 쓴다. 이용약관 전문(695:2753 「이용약관 / 개인정보 및 사주 결과 안내」)은 사주 입력(SCR-02)과 사전신청(SCR-09) 양쪽에서 바텀시트로 연다 — 사전신청 시트는 읽을 거리이고 동의는 체크박스가 받는다(2026-09-16 결정). 사주 입력(SCR-02)의 시트에는 맨 끝에 [동의] 절(''점지 확인'을 누르시면 … 동의한 것으로 간주됩니다')을 둔다(2026-09-17 소유자 결정) — 사전신청 시트에는 두지 않는다 | 소개팅 | Must | US-5 | 이동건 |
| FR-18 | 시스템은 이 브라우저가 만든 사주 결과가 아닌 결과 주소(`/reading/:id`)에 접근하거나, 이 브라우저가 만든 사주 결과가 없는 상태에서 궁합 지도·소개팅 화면에 접근하면 사주 입력(SCR-02)으로 안내해야 한다. 공유 링크 입력(SCR-06)은 안내 대상이 아니고, 결과 없이 친구의 궁합 지도(SCR-13)에 오면 SCR-06 으로 안내한다 | 진입/홈 | Should   | US-4 | 이정진 |
| FR-19 | 시스템은 하단 네비게이션(홈·궁합지도·소개팅)을 viewport 하단에 고정해 세 영역을 잇고, 사주 결과 화면을 홈으로 써야 한다. 홈 탭은 티저로 돌아가지 않고 사주 데이터 유무에 따라 결과 또는 입력으로 분기한다. 공유 Flow 진행 중에는 네비를 노출하지 않고, '내 사주 내용도 확인하기'로 홈에 들어온 시점부터 노출한다. 페이지 최하단이 아니라 viewport 하단이며 모바일 브라우저의 Safe Area 를 고려한다(기능명세서 3.9) | 진입/홈 | Must | US-1, US-4 | 이정진 |
| FR-20 | 시스템은 카카오 로그인을 제공해야 한다. 로그인은 궁합지도 저장·복원(FR-21)과 소개팅(FR-24~FR-31)에만 필요하고, 사주 보기·결과 조회·공유·친구 궁합은 로그인 없이 끝까지 동작한다. 카카오 동의항목은 받지 않고 회원번호만 식별자로 쓴다. 비로그인 사용자의 궁합지도 하단에는 '로그인하고 저장하기' CTA 를 두고, 로그인 사용자에게는 노출하지 않는다(기능명세서 4.9). 메인 티저의 '이미 아이디가 있어요'도 이 로그인으로 온다(FR-1) | 궁합지도/로그인 | Must | US-7 | 곽도윤 |
| FR-21 | 시스템은 로그인 시 계정에 저장된 사주·궁합 기록을 우선 복원해야 한다. 계정이 비어 있을 때만 현재 브라우저의 결과를 계정에 연결하고, 계정에 기록이 있으면 브라우저 기록은 연결하지 않고 둔다. 로그인을 시작한 화면으로 복귀하며, 취소·실패해도 현재 지도와 비로그인 기록은 유지한다 | 궁합지도/로그인 | Must | US-7 | 이정진 |
| FR-22 | 시스템은 친구 궁합 Row 를 누르면 그 상대와의 궁합 이유('왜 나에게 귀인일까요?', '둘이 만나게 된다면?', '둘이 싸우게 된다면?')를 보여줘야 한다. 이유는 등록 시점이 아니라 **최초 열람 시** 생성하고 이후에는 저장된 값을 돌려준다 — 화면은 첫 열람이 수 초 걸릴 수 있음을 로딩으로 알린다 | 궁합지도/로그인 | Must | US-4 | 이정진 |
| FR-23 | 시스템은 공유 링크로 들어온 사용자에게 브라우저에 쓸 수 있는 사주 정보가 있으면 '이전 정보 불러오기'와 '새로 작성하기'를 고르게 해야 한다. 이전 정보를 고르면 재입력 없이 궁합을 만들고, 새로 작성해도 기존 데이터는 지우지 않는다 | 공유/친구궁합 | Must | US-3 | 강근우 |
| FR-24 | 시스템은 소개팅 탭에 들어오면 로그인 여부와 등록 정보로 화면을 나눠야 한다: 비로그인은 인트로(비로그인)로 가고 '로그인하고 내 운명 찾아 떠나기'가 카카오 로그인 시트를 연다 — 시트의 '나중에 할래요'는 시트만 닫는다. 로그인 사용자는 인트로(로그인)에서 '내 운명 찾아 떠나기'로 필요한 입력 단계(FR-25)로 가고, 사주·프로필이 모두 있으면 입력을 반복하지 않고 Top 3 로 간다. 로그인 인트로에는 로그아웃이 있다. 로그인 취소·실패는 지금 화면을 유지하고, 등록 정보 조회가 실패해도 진입을 막지 않고 다시 시도할 수 있게 한다. 로그인하지 않으면 Top 3 조회와 등록을 할 수 없다(기능명세서 6.1~6.4) | 소개팅 | Must | US-5 | 이동건 |
| FR-25 | 시스템은 소개팅 프로필을 두 단계로 등록받아야 한다: (1/2) 사주 정보(성별·달력 기준·생년월일·태어난 시간 또는 '몰라요'·닉네임 1–8자), (2/2) 이름·사진·학교(DGU) 메일·연락 수단(전화번호 또는 인스타그램 **택1**)·학과·MBTI(16유형)·자기소개. 사주 정보가 없으면 1/2 부터, 사주는 있고 프로필이 없으면 2/2 부터 시작하고, 이미 있는 단계는 다시 받지 않는다. 필수값 누락·날짜 형식·이메일 형식·연락처 형식 오류와 사진 업로드 실패는 그 항목에 알리고 다른 입력값은 유지한다. '내 운명 찾아 떠나기'로 저장한 뒤 Top 3 로 간다. 사진은 업로드 후 원본과 썸네일을 따로 보관한다. 학교 메일 인증 방식은 미정이다(Q20)(기능명세서 6.5~6.8) | 소개팅 | Must | US-5 | 이동건 |
| FR-26 | 시스템은 사주 궁합 점수를 기준으로 오늘의 인연 Top 3 를 카드로 보여줘야 한다. 카드는 좌우로 넘기고 아래 인디케이터가 지금 카드 위치를 보인다. 카드 앞면은 Top 순위·관계 유형·점수·MBTI·자기소개를(값이 없는 항목은 비운다), 뒷면은 이름·학과·궁합 까닭을 담되 해금하지 않은 항목(사진 포함)은 값 대신 잠금 상태와 여는 데 드는 실 개수를 보인다. '카드 뒤집기'는 그 카드만 앞뒤를 바꾼다. 추천이 0명이면 '운명의 인연을 기다리고 있어요' 빈 카드를 보이고 넘기기·인연 대상 버튼은 동작하지 않는다. 1–2명일 때의 표시는 미정이다(Q21)(기능명세서 6.9~6.14) | 소개팅 | Must | US-6 | 이동건 |
| FR-27 | 시스템은 추천 3명을 한 번에 바꾸는 '다른 인연 만나보기'를 제공해야 한다 — 하루 한 번은 무료 점지권(자정 초기화)이고 그 뒤에는 실 3개를 쓴다. 바꾸면 직전 3명은 다시 볼 수 없음을 바텀시트로 알리고, '자정까지 기다릴게요'를 고르면 바꾸지 않는다. 무료 점지권이 없으면 Top 3 화면 아래에 다음 무료 점지 시점을 안내한다(기능명세서 6.24~6.27) | 소개팅 | Should | US-6 | 이동건 |
| FR-28 | 시스템은 상대 정보를 항목별로 해금하게 해야 한다 — 사진 10 · 이름 7 · 학과 5 · 궁합 까닭 3. 카드의 '열람하기'가 여는 해금 모달에서 보유 실을 보이고 여러 항목을 함께 고르게 하며, 아무것도 고르지 않으면 '사용하기'를 막고 이미 연 항목은 고를 수 없다. 잔액이 모자라면 해금하지 않는다. 해금이 끝나면 남은 실과 새로 볼 수 있는 항목을 알리고 '확인'으로 카드로 돌아간다. 해금이 실패하면 실을 차감하지 않고 잠금을 유지한다. 같은 상대의 같은 항목은 한 번만 차감하고, 해금 후에는 잠금이 풀린 값(사진은 원본)을 보여주며, 네 항목을 모두 열면 추가 열람 버튼을 더 보이지 않는다. 잠긴 사진의 원본 주소는 화면·응답 어디에도 없어야 한다(기능명세서 6.14~6.19) | 소개팅 | Must | US-8 | 강근우 |
| FR-29 | 시스템은 지금 보고 있는 카드의 '운명의 실 보내기'로 매칭을 요청하게 해야 한다. 열지 않은 항목이 남아 있으면 보내기 전에 '보낸 뒤에는 열지 않은 정보를 더 볼 수 없다'를 확인받고, 취소하면 보내지 않는다. 보낸 뒤에는 그 상대의 미해금 항목을 해금할 수 없고(이미 연 항목은 계속 보인다), 같은 상대에게 다시 보낼 수 없다. 전송이 끝나면 성공 모달을 보이고 '확인'은 닫기, '보러가기'는 요청함의 보낸 신청으로 간다. 전송이 실패하면 요청함에 넣지 않는다(기능명세서 6.20~6.23) | 소개팅 | Must | US-6 | 강근우 |
| FR-30 | 시스템은 소개팅 상단의 요청함에서 요청을 '보낸 신청'과 '받은 신청' 두 탭으로 보여줘야 한다. 목록의 줄을 누르면 상대 카드를 띄운다. 보낸 신청 목록은 상대와의 궁합 점수를 보이고(받은 신청의 점수는 Q17), 신청이 없으면 목록을 그리지 않는다. 보낸 신청은 상대가 응답하기 전에만 '요청 취소'할 수 있고, 거절되거나 상대가 다른 사람과 맺어지면 '매칭에 실패했어요'로 표시하며 다시 요청할 수 없다. 받은 신청의 상대 정보는 실 없이 전부 공개하고 '인연이 되고 싶어요'(수락)·'다음 기회에...'(거절)를 제공하며, 이미 응답한 신청에는 다시 응답할 수 없고 취소된 신청은 상세 접근을 막는다. 수락하면 매칭이 성립하고 서로 등록한 연락 수단(전화번호 또는 인스타그램)을 확인할 수 있다 — 상대가 먼저 취소했으면 성립하지 않는다. 성립 후 화면은 디자인 미정이다(기능명세서 6.28~6.36) | 소개팅 | Must | US-9 | 강근우 |
| FR-31 | 시스템은 재화 '실'의 잔액을 상단에 보이고, 획득(가입 10 · 출석 5 · 친구 1명 등록당 3 · 제휴 FR-32)과 소모(해금·유료 리롤)를 반영해야 한다. 잔액이 모자라면 해금·리롤을 진행하지 않고 부족함을 알린다. 현금 결제는 제공하지 않는다 | 재화 | Must | US-8 | 이동건 |
| FR-32 | 시스템은 축사 협업 페이지의 링크로 들어온 사용자에게 운명의 실 10개를 주고 '운명의 실이 지급되었어요' 모달로 지급 뒤 보유 수를 알려야 한다. 같은 협업 혜택은 한 번만 준다. 모달의 '바로 로그인하고 운명의 짝 찾아보기'는 카카오 로그인으로 가고(취소하면 모달로 돌아온다), 로그인 뒤에는 FR-24·25 와 같이 등록 상태에 맞는 화면으로 간다. '나중에 사용할래요'는 모달을 닫고 서비스 메인으로 간다. 다른 경로로 들어오면 모달은 없다. 진입 식별과 지급 시점은 Q19(기능명세서 7.1~7.6) | 재화 | Should | US-8 | — |

## 선후 관계

V1 요구는 서로를 막는다. 아래가 Notion ⚔️ PRD 보드의 `선행`·`후행` 관계와 같은 내용이다 — 보드가 원본이고
여기는 사본이다. 화살표는 "왼쪽이 서야 오른쪽을 시작할 수 있다"로 읽는다.

```text
NFR-7 JWT 쿠키 ──▶ FR-20 카카오 로그인 ─┬─▶ FR-21 궁합지도 복원
                                        ├─▶ FR-31 실 잔액
                                        └─▶ FR-24 소개팅 진입 ──▶ FR-25 프로필 2단계 ──▶ FR-26 Top 3
FR-19 하단 네비 ───────────────────────────▶ FR-24                                          │
                                                                                            ├─▶ FR-27 리롤
                                                                                            └─▶ FR-28 정보 해금 ──▶ FR-29 운명의 실 ──▶ FR-30 요청함
FR-31 실 잔액 ─────────────────────────────────────────────────────────────────────────────▶ FR-27 · FR-28
```

읽히는 것: `NFR-7`·`FR-20`(카카오 로그인)이 V1 의 거의 전부를 막는다. `FR-19`(하단 네비)만 지금 바로 시작할 수
있고, 소개팅 줄기(`FR-24`~`FR-30`)는 로그인이 선 뒤에야 순서대로 풀린다. `FR-22`·`FR-23`은 로그인과 무관해
독립으로 진행한다.
